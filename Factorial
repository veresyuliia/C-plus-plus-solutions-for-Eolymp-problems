#include <iostream>

using namespace std;

 int n;
int64_t f(int32_t n)
{
    if(n<0)
    {
        return 0;
    }
    if(n==0)
    {
        return 1;
    }
    return n*f(n-1);

}

int main()
{
    cin>>n;
    cout<<f(n);
}
